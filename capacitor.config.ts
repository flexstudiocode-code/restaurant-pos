import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nellara.pos',
  appName: 'Meadows Park Restaurant',
  webDir: 'dist'
};

export default config;
